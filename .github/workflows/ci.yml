name: C++ GCC CI with Makefile

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build with Make
        run: make

      - name: Run Test
        run: ./my_app